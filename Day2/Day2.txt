Enum TrafficLight { RED, GREEN, YELLOW }

Class SignalController:
    CurrentLight = RED
    Method changeLight():
        if CurrentLight == RED → set to GREEN
        else if CurrentLight == GREEN → set to YELLOW
        else if CurrentLight == YELLOW → set to RED


Abstract class Crop:
    method waterRequirement()
    method harvest()

Class Rice extends Crop:
    waterRequirement = "High"
    harvest = "90 days"

Class Wheat extends Crop:
    waterRequirement = "Medium"
    harvest = "120 days"


Array timeSlots[n]
Pointer = 0
For each question:
    start timer
    if time left < 5 sec - trigger warning
    move pointer circularly


Class Job { deadline, processingTime }
Use PriorityQueue ordered by deadline
While queue not empty:
    take job with earliest deadline
    process it


Class Item { name, price }
List<Item> cart
Method totalPrice(cart):
    sum all item.price


Map<Department, List<Doctor>>
Add doctor under department
Print doctors by department
